<PISI>
    <Source>
        <Name>qemu</Name>
        <Homepage>http://wiki.qemu.org/Main_Page</Homepage>
        <Packager>
            <Name>Ikey Doherty</Name>
            <Email>ikey@evolve-os.com</Email>
        </Packager>
        <License>GPL-2.0</License>
        <Summary xml:lang="en">QEMU is a generic and open source machine emulator and virtualizer.</Summary>
        <Description xml:lang="en">QEMU is a generic and open source machine emulator and virtualizer.

When used as a machine emulator, QEMU can run OSes and programs made for
one machine (e.g. an ARM board) on a different machine (e.g. your own PC).
By using dynamic translation, it achieves very good performance.

When used as a virtualizer, QEMU achieves near native performances by
xecuting the guest code directly on the host CPU. QEMU supports
virtualization when executing under the Xen hypervisor or using the
KVM kernel module in Linux. When using KVM, QEMU can virtualize x86,
server and embedded PowerPC, and S390 guests.
</Description>
        <Archive type="binary" sha1sum="083029a13c0c475bf42a4232c68a9caa6dbc763e">https://evolve-os.com/sources/README</Archive>
    </Source>
    <Package>
        <Name>qemu</Name>
        <Summary xml:lang="en">QEMU is a generic and open source machine emulator and virtualizer.</Summary>
        <Description xml:lang="en">QEMU is a generic and open source machine emulator and virtualizer.

When used as a machine emulator, QEMU can run OSes and programs made for
one machine (e.g. an ARM board) on a different machine (e.g. your own PC).
By using dynamic translation, it achieves very good performance.

When used as a virtualizer, QEMU achieves near native performances by
xecuting the guest code directly on the host CPU. QEMU supports
virtualization when executing under the Xen hypervisor or using the
KVM kernel module in Linux. When using KVM, QEMU can virtualize x86,
server and embedded PowerPC, and S390 guests.
</Description>
        <PartOf>virt</PartOf>
        <Files>
            <Path fileType="data">/usr/share/qemu</Path>
            <Path fileType="data">/usr/share/qemu/keymaps</Path>
            <Path fileType="doc">/usr/share/doc/qemu</Path>
            <Path fileType="man">/usr/share/man</Path>
            <Path fileType="executable">/usr/bin</Path>
            <Path fileType="library">/usr/lib64/qemu/qemu-bridge-helper</Path>
            <Path fileType="config">/etc/qemu/target-x86_64.conf</Path>
        </Files>
    </Package>
    <History>
        <Update release="1">
            <Date>2015-03-11</Date>
            <Version>2.2.1</Version>
            <Comment>Packaging update</Comment>
            <Name>Ikey Doherty</Name>
            <Email>ikey@evolve-os.com</Email>
        </Update>
    </History>
</PISI>